# B题模拟器检测指导

## 1. 目的与证据边界

本文件说明如何把 `b_solution` 中论文的搜索、测向、定位和清除算法接入 B 题官方模拟器进行演练与正式测试。

**当前状态：官方模拟器尚未成功运行，且按要求搁置。本文是未来获授权后使用的操作指导，不表示当前GUI、登录或接口已可用；现在不要据此下载、启动或调用官方模拟器。** 六次正式结果、六份加密日志及真实参赛队人工核验仍未完成。

根据附件1和附件2，官方模拟器只提供以下四个 HTTP+JSON 接口：

- `POST /enter`
- `POST /measure`
- `POST /clear`
- `POST /exit`

模拟器会为每局测试随机生成隐藏的干扰源案例，不提供导入坐标、接收半径、定向方向、CSV、随机种子或旧轨迹的接口。因此，论文中的研究数据不能直接“上传到模拟器”。正确做法是：

> 让论文算法读取模拟器的公开动作响应，并在线决定下一步动作；用模拟器反馈、耗时、清除结果和官方行为日志检验算法。

`b_solution/results/` 中的合成数据、轨迹和统计量仅用于本地研究和算法对照，不是官方案例数据，不得作为正式测试成绩或正式日志使用。

## 2. 论文方法与模拟器的对应关系

| 论文内容 | 模拟器中的检验方式 |
| --- | --- |
| 问题1：有误差示向度下的交会定位区域 | 每次收到 `direction` 后，按题面±1°加两位小数量化半单位0.005°，构造±1.005°保守半平面交，维护定位可行域并检查清除判据 |
| 问题1：定位区域直径和最小包围圆 | 在本地几何程序中直接验证；官方接口不返回真实源坐标，不能在正式案例中直接计算真实定位误差 |
| 问题2：第二检测点候选区域和评分 | 作为在线局部测向的选点依据；用清除成功率、失败清除次数和完整任务耗时评价 |
| 问题3：全向源搜索 | `refined` 使用原点加六个半径1200 m环点的七点骨架，center联合调度、unknown扫描；未知频道保留完整发现义务 |
| 问题4：全向与定向源混合搜索 | `refined` 使用compact25：原点、12个940 m内环点、12个1870 m外环点，外环错相15°；guarded联合调度、useful扫描、有限恢复和rectangle光学后备 |
| 本地实验表格和轨迹 | 用于同种子配对比较、计时复算和程序诊断，不能导入官方模拟器或替代正式结果 |

论文相关代码和说明：

- `b_solution/planner.py`：默认 `refined` 策略及在线状态机；历史 `enhanced` 保留Q3七点，以及Q4的31点990 m格点和rolling/separate/all作为对照，不改写旧结果；
- `b_solution/official_run.py`：连接已经启动的官方 GUI 测试；
- `b_solution/client.py`：HTTP、幂等重试、串行动作和明文过程日志；
- `b_solution/polar_coverage_candidate.py`：25点构造及三角剖分覆盖证书；
- `b_solution/verify_research.py`：新覆盖和策略行为的独立本地核验；
- `b_solution/report/paper.tex`：问题1/2几何、问题3/4搜索、计时及状态机；
- `b_solution/report/research_theory.tex`：新覆盖、扫描证书及联合调度理论。

## 3. 官方测试前的准备

### 3.1 模拟器准备

以下步骤仅用于以后官方服务可用且获授权恢复测试时。当前没有成功官方测试记录。

1. 下载并解压附件1给出的模拟器，提取码为 `2026`。
2. 在模拟器 GUI 中登录，使用真实参赛队号。
3. 模拟器与控制程序运行在同一台电脑上。默认接口为：

   ```text
   http://127.0.0.1:2026
   ```

4. 如果在模拟器空闲时修改了端口，命令中的 `--base-url` 必须同步修改。
5. 启动测试模块后，等待案例准备完成、5秒倒计时结束，并确认界面显示机器狗接口已经就绪。
6. 不要在接口未开放时用脚本反复重试；倒计时期间连接失败属于正常现象。
7. 按附件1第2.5节，倒计时结束后开启**25分钟现实测试窗口**；成功 `/enter` 后程序运行最多**20分钟**，两者较早到达即终止；虚拟时间另有**100小时**上限。不要把论文虚拟耗时或本地复现实验超时上限当作现实预算。
8. 附件1第4.2节规定：**北京时间2026年9月13日17:30之后不能启动新的演练或正式测试**，此前开始的可以完成；建议在当日15:30前完成正式测试。此日期来自附件原文，不是当前服务可用性判断。正式测试开始成功即占用机会，每问三次，中止也占用。

### 3.2 控制程序准备

- 只能由一个进程驱动当前测试。
- `--robot-id` 必须逐字节等于 GUI 当前登录的真实参赛队号。
- 每个新动作使用新的 `request_id`。
- 网络超时或连接中断后重试同一动作，必须复用原请求内容和原 `request_id`。
- 收到完整响应后才能发送下一个不同动作。
- 每次运行都要使用一个**尚不存在**的新输出目录；`official_run.py` 会自行创建该目录。
- 正式测试前不要修改默认策略参数；先用不限次数演练检查全清和计时。

## 4. 启动问题3演练

先在 GUI 中选择“问题3演练测试”，确认开始并等待接口就绪。然后从仓库根目录执行：

```bash
conda run -n py314 python b_solution/official_run.py \
  --problem 3 \
  --robot-id "实际参赛队号" \
  --base-url http://127.0.0.1:2026 \
  --output-dir "$HOME/cumcm_private/q3_practice_01"
```

如果使用默认端口，`--base-url` 可以省略：

```bash
conda run -n py314 python b_solution/official_run.py \
  --problem 3 \
  --robot-id "实际参赛队号" \
  --output-dir "$HOME/cumcm_private/q3_practice_01"
```

## 5. 启动问题4演练

上一局结束后，在 GUI 中重新选择“问题4演练测试”，确认开始并等待接口就绪。使用新的输出目录执行：

```bash
conda run -n py314 python b_solution/official_run.py \
  --problem 4 \
  --robot-id "实际参赛队号" \
  --base-url http://127.0.0.1:2026 \
  --output-dir "$HOME/cumcm_private/q4_practice_01"
```

`--problem 3/4` 只决定本地 Planner 使用哪套算法，不能替代 GUI 中选择测试模块。GUI 选择问题3而命令写 `--problem 4` 会造成题号和算法不匹配，必须避免。

## 6. 在线反馈的处理规则

### 6.1 `/measure` 响应

只在 `accepted=true` 时更新程序状态和虚拟时间。

#### `measure_result="direction"`

响应包括 `svd_deg`。它是带误差的示向度，不是精确真实方位。程序应：

1. 将角度转换为弧度参与计算；
2. 按题面±1°与两位小数舍入误差0.005°之和，建立±1.005°保守方向区域；
3. 更新对应频道的定位可行域；
4. 根据当前可行域选择后续测点或清除点。

#### `measure_result="near"`

检测距离不超过5 m且位于信号覆盖范围内，没有返回示向度。此时可以直接在当前位置尝试 `/clear`。

#### `measure_result="no_signal"`

只能说明当前位置收不到该频道信号，不能说明该频道一定没有干扰源。原因可能是：

- 源已被清除；
- 超出该源的有效接收半径；
- 定向源的覆盖半平面没有覆盖当前检测点。

问题4中不能因为单个圆内点 `no_signal` 就停止搜索，必须保留完整方向无关覆盖证书和圆外检测点。

### 6.2 `/clear` 响应

`/clear` 的 `channel` 是要尝试清除的目标频道，不是测向机要切换到的频道。

- `clear_result="success"`：把该频道记为已清除；每个频道只计一次。
- `clear_result="no_target_in_range"`：当前位置20 m范围内未发现目标，不能计入成功清除数。

`/clear` 不改变测向机当前频道，也不产生频道切换耗时。

### 6.3 位置、频道和角度规则

- 目标源均在半径1800 m圆域内，但机器狗可以提交圆域外的检测位置；
- 机器狗移动按前后两个合法动作位置的欧氏距离计算；
- 速度为5 m/s；
- 测向机初始频道为1；
- 只有合法 `/measure` 的频道变化产生1秒切换耗时；
- 每次检测动作耗时5秒；
- 失败 `/clear` 耗时3秒，成功 `/clear` 耗时5秒；
- 清除半径为20 m；
- 坐标单位为米，坐标分量必须为有限数且绝对值不超过2000000；
- 东为0°，北为90°，角度逆时针为正，范围为[0°,360°)。

## 7. 停止条件与安全证书

不能用“已经发现的频道都清除了”作为结束条件，因为总源数未知。

论文程序的安全停止逻辑是：

1. 已成功清除的频道状态为 `cleared`；
2. 尚未发现目标的频道，必须完成本题全部覆盖骨架点的有效检测义务，只有完整无信号记录才能标记为 `absent-certified`；Q3为七点，Q4为具有方向无关证明的25点，不能仅检查已访问点；
3. 已经收到过 `direction` 或 `near` 的频道不能被后续 `no_signal` 改写为无源；
4. 当所有频道均为 `cleared` 或 `absent-certified` 时，才允许主动 `/exit`；
5. 成功清除16个不同频道时，可以利用题目给出的源数上界提前结束。

`unknown`（Q3默认）跳过已有定位轨迹的频道；`useful`（Q4默认）仅在已知频道包围圆半径不超过19.9 m，或骨架点到圆心距离减包围半径大于1500 m时跳过该重复扫描。跳过不是观测，不能增加 `scan_records` 或生成无源证据。所有未知频道仍完成原有发现义务；已知目标进入定位清除流程，不因跳过而变成无源。固定探测与覆盖整个可行多边形的rectangle光学后备仍保留。

正常的覆盖证书结束时，应看到：

```text
certificate_complete = true
termination = all_channels_cleared_or_coverage_certified_absent
```

如果成功清除16个不同频道，程序也可能按题目给出的数量上界提前结束，此时 `termination` 可以是 `known_count_upper_bound_16`，但仍应确认 `certificate_complete=true`。

如果遇到网络超时、连接中断或结果不确定，不要立即发送新的动作。先检查明文日志和 GUI 状态；同一动作只能用同一请求体和同一 `request_id` 重试。重试耗尽后，客户端会进入不确定状态，不能把该动作擅自当作“未执行”。

## 8. 时间和结果统计

### 8.1 虚拟时间

对所有被接受的唯一动作，独立复算：

\[
T=\frac{L}{5}+5M+S+3F+5K,
\]

其中：

- \(L\)：总移动距离；
- \(M\)：被接受的 `/measure` 次数；
- \(S\)：实际频道切换次数；
- \(F\)：失败 `/clear` 次数；
- \(K\)：成功清除的不同频道数。

不要按日志中的请求行数统计，因为网络重试会产生多条请求记录但只对应一个动作。

### 8.2 演练测试

演练结束后，GUI可以显示该案例的真实总源数 \(N\)。记录并计算：

\[
\text{清除率}=K/N,
\qquad
\text{平均定位清除时间}=T/K.
\]

演练的主要通过条件：

- `K=N`；
- `certificate_complete=true`；
- 没有因超时或技术异常结束；
- 计时公式复算结果与最后接受动作的虚拟时间一致；
- 极端定向场景下仍能完成全清。

建议至少覆盖普通案例、最小接收半径、边界外向定向源和聚集源等压力场景。不同演练局由模拟器随机生成，不能把不同案例直接当作严格配对实验。

### 8.3 正式测试

正式测试中总源数 \(N\) 不公开，论文表格只填写：

- 案例编码；
- 成功清除数 \(K\)；
- 平均定位清除时间 \(T/K\)；
- 程序运行时间。

正式测试不能填写 \(K/N\)，也不能把演练总源数当作正式案例分母。

程序运行时间按模拟器定义，从 `/enter` 成功到测试结束的现实时间计算。正常主动退出时，可以用被接受的 `/enter` 和 `/exit` 的 `real_timestamp_ms` 作辅助核对；超时或手工中止时没有可依赖的 `/exit` 响应，应以 GUI 显示的结束时间为准。

现实20/25分钟与虚拟100小时分别受限。即使理论终止界或本地合成全清成立，也不能据此承诺在任一官方案例的现实期限内完成；演练时应同时核对GUI现实计时及网络/计算开销。

## 9. 输出文件和隐私

每次 `official_run.py` 运行成功后，输出目录主要包含：

- `private_requests.jsonl`：明文请求、响应、重试和接受动作记录；
- `observed_result.json`：Planner结果和入口元数据。

`private_requests.jsonl` 含真实 `robot_id`，是私有过程证据，不是官方加密行为日志。正式测试还必须在模拟器 GUI 中导出官方原名、原字节的加密日志；问题3和问题4共六份，不能用本地 JSONL 替代。

`observed_result.json` 中如果出现以下内容，不得直接填入正式论文表格：

- `case_code` 的占位提示；
- `total_source_count` 的“未公开”提示；
- `local_wall_duration_s`，它是控制程序墙钟时间，不等于官方 GUI 的程序运行时间。

真实案例编码必须从模拟器 GUI 或日志列表抄录。

## 10. 正式测试操作清单

### 每局开始前

- [ ] 已在 GUI 登录正确参赛队号；
- [ ] 已选择正确题号和演练/正式模块；
- [ ] 已确认测试开始；
- [ ] 已等待倒计时结束并看到接口就绪；
- [ ] 输出目录尚不存在；
- [ ] 只启动一个控制程序；
- [ ] 已冻结算法和参数。

### 每局结束后

- [ ] 已从 GUI 记录案例编码；
- [ ] 已记录结束原因；
- [ ] 已核对成功清除频道数；
- [ ] 已计算或核对 \(T/K\)；
- [ ] 已记录官方程序运行时间；
- [ ] 已导出官方加密日志；
- [ ] 已检查日志上传状态；
- [ ] 未将真实队号明文日志放入公开材料。

### 三次正式测试完成后

分别对问题3和问题4填写三行结果，共六行。不要使用占位案例编码，不要把本地合成实验结果写成正式测试结果。

## 11. 常见错误

| 错误 | 后果 | 正确处理 |
| --- | --- | --- |
| 把论文坐标轨迹上传到模拟器 | 官方接口不支持，无法执行 | 只将算法接入 `/measure` 和 `/clear` |
| 把 `no_signal` 当作无源 | 定向源和超接收半径源会漏检 | 完整覆盖后才建立 `absent-certified` |
| 把 `svd_deg` 当作精确方位 | 定位区域过度收缩，可能清除失败 | 按±1.005°覆盖题面误差及量化误差 |
| 用 `/clear` 频道判断测向机频道 | 频道状态错误、计时错误 | 只有 `/measure` 改变测向机频道 |
| 重试时换新 `request_id` | 同一动作可能重复执行 | 原请求内容和原ID一起重试 |
| 只检查 HTTP 200 | `accepted=false`动作并未执行 | 同时检查 HTTP状态和 `accepted` |
| 把拒绝响应的虚拟时间0当当前时间 | 时间状态被错误重置 | 只采用最近一次 `accepted=true` 的时间 |
| 正式测试填写 `K/N` | 正式接口不公开N | 正式表只填K和T/K |
| 用本地 JSONL 替代官方加密日志 | 不满足官方支撑材料要求 | 从 GUI 原名导出官方加密日志 |
| 同一输出目录重复运行 | 覆盖或混淆不同测试证据 | 每局使用全新的目录 |

## 12. 研究证据与官方证据的区分

当前仓库已有的本地合成运行可以用于：

- 检查算法能否从公开反馈完成动作闭环；
- 检查问题1、问题2的几何计算和定位逻辑；
- 比较策略的完整虚拟耗时；
- 检查失败清除恢复、边界搜索和通信重试；
- 生成论文中的非官方实验图表。

最新冻结研究为10700次执行（主比较7000、压力1800、替代误差1600、敏感性300），不是10700个独立世界；主组每问1000世界。2550次候选开发与历史4430/3080/2200次执行分开统计。Q4聚集场景极端误差的50对结果中，refined平均反而慢8.1352秒/源，不能宣传逐场景或逐实例占优；详见 `results/research_experiments.json` 和复现说明。任何本地收益都不是官方成绩或获奖预测。

它们不能证明：

- 官方案例中一定达到相同耗时；
- 官方案例分布与本地合成分布相同；
- 已经完成问题3、问题4的三次正式测试；
- 已经获得官方案例编码或加密日志。

只有模拟器 GUI 的实际测试记录、官方导出的加密日志和按正式表格口径整理的六次正式结果，才能作为正式测试证据。

## 13. 参考文件

- `附件1.docx`：模拟器使用说明、GUI流程、测试次数、日志和截止规则；
- `附件2.docx`：HTTP接口、反馈字段、计时、重试和错误状态；
- `b_solution/official_run.py`：官方会话连接入口；
- `b_solution/client.py`：串行 HTTP 客户端和日志；
- `b_solution/planner.py`：论文搜索与清除策略；
- `b_solution/run.py`：本地合成环境入口；
- `b_solution/reproduce.txt`：本地研究复现命令和官方测试限制。
