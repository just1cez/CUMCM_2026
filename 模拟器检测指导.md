# B题模拟器检测指导

## Windows 快速开始：先确认新版，再开演练

**先完成下面的更新和离线检查，再在模拟器中开始测试。** 不要为检查环境消耗正式机会。

### A. 更新代码或取得完整支撑包

- Git克隆用户：在仓库根目录、`main` 分支上执行 `git pull --ff-only`。如提示本地改动或分支分歧，先保留并处理自己的修改；不要使用强制重置或清理命令来覆盖日志。
- 不使用Git：从仓库的 `b_solution/delivery/B题研究支撑材料.zip` 下载并**完整解压到新文件夹**。不要只复制 `official_run.py`，它依赖同目录下的规划器、几何和通信模块。
- 当前代码入口在根仓库的 `b_solution/`。交接目录 `ai_optimization_handoff_20260912/` 用于保存演练证据，其重复代码快照已清理，不再从那里启动控制器。
- 已有的模拟器数据目录与私有运行日志目录应保留；更新策略代码不需要删除它们。

### B. 在PowerShell中做不联网的程序检查

以下命令从**仓库根目录**执行；若使用解压后的支撑包，从解压根执行，并去掉每条命令的 `b_solution/` 前缀：

```powershell
conda run -n py314 python --version
conda run -n py314 python b_solution/official_run.py --help
conda run -n py314 python b_solution/run.py --problem 3 --strategy field --seed 20260910
conda run -n py314 python b_solution/run.py --problem 4 --strategy field --seed 20260910
```

帮助输出应包含 `--strategy {field,refined}`。两条 `run.py` 命令只运行本地合成环境，不连接官方模拟器、不占正式机会；检查输出的 `strategy="field"`、`certificate_complete=true` 和 `fraction_cleared=1.0`。不要把这些本地输出当作新的官方成绩。

如果旧程序已经能在 `py314` 环境运行，不必重建环境。只有提示环境不存在时，才创建：

```powershell
conda create -n py314 python=3.14 -y
```

在线控制器及上述单局运行使用Python标准库；完整重算论文、绘图和PDF检查才需要按 `b_solution/reproduce.txt` 安装 `requirements.txt` 中的依赖。若PowerShell找不到 `conda`，先配置已安装的Miniconda/Anaconda的PowerShell支持，或使用其PowerShell终端。

### C. 再运行官方演练

在模拟器GUI登录并选择正确题号的**演练测试**，等倒计时结束、接口就绪，再执行第4或第5节的命令。建议显式写 `--strategy field`，原算法对照写 `--strategy refined`。

每局必须使用不同且尚不存在的输出目录。不要在同一局同时启动两个策略；也不要先手工调用 `/enter`。本指南中的 `$HOME` 写法用于PowerShell，不是CMD的变量语法。

结束后同时保留 `private_requests.jsonl`、`observed_result.json` 和模拟器对应结果，记录真实案例编码。先确认新版在新的官方演练中全清，再决定正式测试所用策略。不同演练局是不同随机案例，不能把两局时间差直接当作严格配对收益。

快速定位：第4/5节为启动命令，第8节为指标口径，第9节为日志保存，第11节为常见错误。

## 1. 目的与证据边界

本文件说明如何把 `b_solution` 中论文的搜索、测向、定位和清除算法接入 B 题官方模拟器进行演练与正式测试。

**当前状态：已获得用户提供的Q3/Q4各10轮官方演练脱敏记录，已用于离线成本分析；默认入口升级为 `field`，20轮所用原算法保留为 `--strategy refined`，两题探测比例均为0.22。新策略本地收益不是官方提升。** 六次正式结果、六份加密日志及真实参赛队人工核验仍未完成；本指南不授权自动启动官方测试或额外消耗正式机会。

根据附件1和附件2，官方模拟器只提供以下四个 HTTP+JSON 接口：

- `POST /enter`
- `POST /measure`
- `POST /clear`
- `POST /exit`

模拟器会为每局测试随机生成隐藏的干扰源案例，不提供导入坐标、接收半径、定向方向、CSV、随机种子或旧轨迹的接口。因此，论文中的研究数据不能直接“上传到模拟器”。正确做法是：

> 让论文算法读取模拟器的公开动作响应，并在线决定下一步动作；用模拟器反馈、耗时、清除结果和官方行为日志检验算法。

`b_solution/results/` 中的合成数据、轨迹和统计量仅用于本地研究和算法对照，不是官方案例数据，不得作为正式测试成绩或正式日志使用。

## 2. 论文方法与模拟器的对应关系

| 论文内容 | 模拟器中的检验方式 |
| --- | --- |
| 问题1：有误差示向度下的交会定位区域 | 每次收到 `direction` 后，按题面±1°加两位小数量化半单位0.005°，构造±1.005°保守半平面交，维护定位可行域并检查清除判据 |
| 问题1：定位区域直径和最小包围圆 | 在本地几何程序中直接验证；官方接口不返回真实源坐标，不能在正式案例中直接计算真实定位误差 |
| 问题2：第二检测点候选区域和评分 | 作为在线局部测向的选点依据；用清除成功率、失败清除次数和完整任务耗时评价 |
| 问题3：全向源搜索 | 默认 `field` 保留七点1200 m环/center/unknown，加入路线组合、定位前条件式原地一次补测、仅Q3的保守无信号几何与安全近侧清除；探测比例0.22 |
| 问题4：全向与定向源混合搜索 | 默认 `field` 保留compact25（原点、12个940 m内点、12个1870 m外点，错相15°）/guarded/useful，仅增加路线组合及条件式原地补测；不作负圆盘推断、不启用近侧清除，探测比例0.22 |
| 本地实验表格和轨迹 | 用于同种子配对比较、计时复算和程序诊断，不能导入官方模拟器或替代正式结果 |

论文相关代码和说明：

- `b_solution/field_policy.py`：入口默认field；`b_solution/planner.py`：refined原算法及在线状态机，两题探测比例0.22。历史enhanced保留Q3七点/Q4的31点990 m格点等对照，不改写旧结果；
- `b_solution/negative_geometry.py`、`b_solution/route_portfolio.py`：Q3保守负反馈几何、有界路线组合；
- `b_solution/official_run.py`：连接已经启动的官方 GUI 测试；
- `b_solution/client.py`：HTTP、幂等重试、串行动作和明文过程日志；
- `b_solution/polar_coverage_candidate.py`：25点构造及三角剖分覆盖证书；
- `b_solution/verify_research.py`：新覆盖和策略行为的独立本地核验；
- `b_solution/report/paper.tex`：问题1/2几何、问题3/4搜索、计时及状态机；
- `b_solution/report/research_theory.tex`：新覆盖、扫描证书及联合调度理论。
- `b_solution/results/official_practice_analysis.json`：20轮用户提供的官方演练观察离线分析，不是正式成绩；
- `b_solution/official_analysis.py`：唯一规范的演练原始日志分析入口，默认读取相邻handoff；脱敏包内运行时指定 `--handoff practice_data`；
- `b_solution/field_study.py`、`field_confirmation.py`、`verify_field.py`：2250次开发、9400次新世界确认及有限核验。fast25仅为内部负候选，无 `--coverage-policy` CLI，不推荐耗用官方机会。

## 3. 官方测试前的准备

以下步骤适用于继续进行官方演练或在授权后恢复正式测试。当前已有用户提供的Q3/Q4各10轮演练观察，正式测试记录仍未完成。

### 3.1 模拟器准备

1. 从[附件1给出的模拟器下载地址](https://pan.baidu.com/s/1P1yfVjY0RufU93XOdzhOLw?pwd=2026)下载并完整解压，提取码为 `2026`。模拟器为绿色软件；运行解压目录中的可执行文件，后续需要移动时应整体移动目录，不要只移动 `.exe`。
2. 在模拟器 GUI 中登录，使用真实参赛队号。
3. 模拟器与控制程序运行在同一台电脑上。默认接口为：

   ```text
   http://127.0.0.1:2026
   ```

4. 如果在模拟器空闲时修改了端口，命令中的 `--base-url` 必须同步修改。
5. 启动测试模块后，等待案例准备完成、5秒倒计时结束，并确认界面显示机器狗接口已经就绪。
6. 不要在接口未开放时用脚本反复重试；倒计时期间连接失败属于正常现象。
7. 按附件1第2.5节，倒计时结束后开启**25分钟现实测试窗口**；成功 `/enter` 后程序运行最多**20分钟**，两者较早到达即终止；虚拟时间另有**100小时**上限。不要把论文虚拟耗时或本地复现实验超时上限当作现实预算。
8. 附件1第4.2节规定：**北京时间2026年9月13日17:30之后不能启动新的演练或正式测试**，此前开始的可以完成；建议在当日15:30前完成正式测试。此日期来自附件原文，不是当前服务可用性判断。正式测试开始成功即占用机会，每问三次，中止也占用。

### 3.2 控制程序准备

- 只能由一个进程驱动当前测试。
- `--robot-id` 必须逐字节等于 GUI 当前登录的真实参赛队号。
- 每个新动作使用新的 `request_id`。
- 网络超时或连接中断后重试同一动作，必须复用原请求内容和原 `request_id`。
- 收到完整响应后才能发送下一个不同动作。
- 每次运行都要使用一个**尚不存在**的新输出目录；`official_run.py` 会自行创建该目录。
- 正式测试前不要修改默认策略参数；先用不限次数演练检查全清和计时。

## 4. 启动问题3演练（Windows PowerShell）

先在 GUI 中选择“问题3演练测试”，等待案例准备和倒计时结束、接口就绪。从仓库根目录选择下面**一个**命令执行；一行命令无需 Bash 的反斜杠续行。`实际参赛队号`必须换成GUI登录值，输出目录必须尚不存在。

默认新策略（显式写field方便留痕）：

```powershell
conda run -n py314 python b_solution/official_run.py --problem 3 --strategy field --robot-id "实际参赛队号" --output-dir "$HOME/cumcm_private/q3_field_practice_01"
```

如需要原算法对照，必须等上一局结束，另在GUI开启新的问题3演练并等待就绪，再使用另一全新目录：

```powershell
conda run -n py314 python b_solution/official_run.py --problem 3 --strategy refined --robot-id "实际参赛队号" --output-dir "$HOME/cumcm_private/q3_refined_practice_01"
```

## 5. 启动问题4演练（Windows PowerShell）

先结束上一局，在 GUI 选择“问题4演练测试”并等待接口就绪。选择新策略：

```powershell
conda run -n py314 python b_solution/official_run.py --problem 4 --strategy field --robot-id "实际参赛队号" --output-dir "$HOME/cumcm_private/q4_field_practice_01"
```

如需旧算法对照，另开新的问题4演练并等待就绪：

```powershell
conda run -n py314 python b_solution/official_run.py --problem 4 --strategy refined --robot-id "实际参赛队号" --output-dir "$HOME/cumcm_private/q4_refined_practice_01"
```

省略 `--strategy` 即field；默认地址为 `http://127.0.0.1:2026`，改端口时添加 `--base-url`。解压支撑包则从解压根目录运行并去掉 `b_solution/` 前缀。每局只允许一个控制进程，不能同局先后跑两策略，不能用新enter续局。每次将01递增或另起全新目录；不同随机案例不是严格配对。

`--problem`必须与GUI题号一致，命令不替代GUI模块选择。正式机会仍为每问三次，成功启动即占用、中止不返还；以上对照是演练用法，不授权增加正式运行次数。fast25仅为内部负候选，临时 `--coverage-policy` 已移除，不为此建议额外官方测试。

## 6. 在线反馈的处理规则

### 6.1 `/measure` 响应

只在 `accepted=true` 时更新程序状态和虚拟时间。

#### `measure_result="direction"`

响应包括 `svd_deg`。它是带误差的示向度，不是精确真实方位。程序应：

1. 将角度转换为弧度参与计算；
2. 按题面±1°与两位小数舍入误差0.005°之和，建立±1.005°保守方向区域；
3. 更新对应频道的定位可行域；
4. 根据当前可行域选择后续测点或清除点。

#### `measure_result="near"`

检测距离不超过5 m且位于信号覆盖范围内，没有返回示向度。此时可以直接在当前位置尝试 `/clear`。

#### `measure_result="no_signal"`

只能说明当前位置收不到该频道信号，不能说明该频道一定没有干扰源。原因可能是：

- 源已被清除；
- 超出该源的有效接收半径；
- 定向源的覆盖半平面没有覆盖当前检测点。

问题4中不能因为单个圆内点 `no_signal` 就停止搜索，必须保留完整方向无关覆盖证书和圆外检测点。
Q3全向情形的field可用 `no_signal` 作保守局部更新：只排除1000米位置盘的内接16边形，再保留剩余可行区域的凸外包。它不等于无源认证，也不取消未知频道完整骨架义务；此推断严禁用于Q4。

### 6.2 `/clear` 响应

`/clear` 的 `channel` 是要尝试清除的目标频道，不是测向机要切换到的频道。

- `clear_result="success"`：把该频道记为已清除；每个频道只计一次。
- `clear_result="no_target_in_range"`：当前位置20 m范围内未发现目标，不能计入成功清除数。

`/clear` 不改变测向机当前频道，也不产生频道切换耗时。

### 6.3 位置、频道和角度规则

- 目标源均在半径1800 m圆域内，但机器狗可以提交圆域外的检测位置；
- 机器狗移动按前后两个合法动作位置的欧氏距离计算；
- 速度为5 m/s；
- 测向机初始频道为1；
- 只有合法 `/measure` 的频道变化产生1秒切换耗时；
- 每次检测动作耗时5秒；
- 失败 `/clear` 耗时3秒，成功 `/clear` 耗时5秒；
- 清除半径为20 m；
- 坐标单位为米，坐标分量必须为有限数且绝对值不超过2000000；
- 东为0°，北为90°，角度逆时针为正，范围为[0°,360°)。

## 7. 停止条件与安全证书

不能用“已经发现的频道都清除了”作为结束条件，因为总源数未知。

论文程序的安全停止逻辑是：

1. 已成功清除的频道状态为 `cleared`；
2. 尚未发现目标的频道，必须完成本题全部覆盖骨架点的有效检测义务，只有完整无信号记录才能标记为 `absent-certified`；Q3为七点，Q4为具有方向无关证明的25点，不能仅检查已访问点；
3. 已经收到过 `direction` 或 `near` 的频道不能被后续 `no_signal` 改写为无源；
4. 当所有频道均为 `cleared` 或 `absent-certified` 时，才允许主动 `/exit`；
5. 成功清除16个不同频道时，可以利用题目给出的源数上界提前结束。

`unknown`（Q3默认）跳过已有定位轨迹的频道；`useful`（Q4默认）仅在已知频道包围圆半径不超过19.9 m，或骨架点到圆心距离减包围半径大于1500 m时跳过该重复扫描。跳过不是观测，不能增加 `scan_records` 或生成无源证据。所有未知频道仍完成原有发现义务；已知目标进入定位清除流程，不因跳过而变成无源。固定探测与覆盖整个可行多边形的rectangle光学后备仍保留。

正常的覆盖证书结束时，应看到：

```text
certificate_complete = true
termination = all_channels_cleared_or_coverage_certified_absent
```

如果成功清除16个不同频道，程序也可能按题目给出的数量上界提前结束，此时 `termination` 可以是 `known_count_upper_bound_16`，但仍应确认 `certificate_complete=true`。

如果遇到网络超时、连接中断或结果不确定，不要立即发送新的动作。先检查明文日志和 GUI 状态；同一动作只能用同一请求体和同一 `request_id` 重试。重试耗尽后，客户端会进入不确定状态，不能把该动作擅自当作“未执行”。

## 8. 时间和结果统计

### 8.1 虚拟时间

对所有被接受的唯一动作，独立复算：

\[
T=\frac{L}{5}+5M+S+3F+5K,
\]

其中：

- \(L\)：总移动距离；
- \(M\)：被接受的 `/measure` 次数；
- \(S\)：实际频道切换次数；
- \(F\)：失败 `/clear` 次数；
- \(K\)：成功清除的不同频道数。

不要按日志中的请求行数统计，因为网络重试会产生多条请求记录但只对应一个动作。

### 8.2 演练测试

演练结束后，GUI可以显示该案例的真实总源数 \(N\)。记录并计算：

\[
\text{清除率}=K/N,
\qquad
\text{平均定位清除时间}=T/K.
\]

演练的主要通过条件：

- `K=N`；
- `certificate_complete=true`；
- 没有因超时或技术异常结束；
- 计时公式复算结果与最后接受动作的虚拟时间一致；
- 极端定向场景下仍能完成全清。

最小接收半径、边界外向定向源和聚集源等指定压力场景，应在本地 `run.py` 合成环境中检查，命令见 `b_solution/reproduce.txt`。官方模拟器自行随机生成隐藏案例，没有导入世界或选择这些源参数的接口；只能记录实际获得的演练结果，不能把不同官方案例当作严格配对实验。

### 8.3 正式测试

正式测试中总源数 \(N\) 不公开，论文表格只填写：

- 案例编码；
- 成功清除数 \(K\)；
- 平均定位清除时间 \(T/K\)；
- 程序运行时间。

正式测试不能填写 \(K/N\)，也不能把演练总源数当作正式案例分母。

程序运行时间按模拟器定义，从 `/enter` 成功到测试结束的现实时间计算。正常主动退出时，可以用被接受的 `/enter` 和 `/exit` 的 `real_timestamp_ms` 作辅助核对；超时或手工中止时没有可依赖的 `/exit` 响应，应以 GUI 显示的结束时间为准。

现实20/25分钟与虚拟100小时分别受限。即使理论终止界或本地合成全清成立，也不能据此承诺在任一官方案例的现实期限内完成；演练时应同时核对GUI现实计时及网络/计算开销。

## 9. 输出文件和隐私

每次 `official_run.py` 运行成功后，输出目录主要包含：

- `private_requests.jsonl`：明文请求、响应、重试和接受动作记录；
- `observed_result.json`：Planner结果和入口元数据。

`private_requests.jsonl` 含真实 `robot_id`，是私有过程证据，不是官方加密行为日志。正式测试还必须在模拟器 GUI 中导出官方原名、原字节的加密日志；问题3和问题4共六份，不能用本地 JSONL 替代。

`observed_result.json` 中如果出现以下内容，不得直接填入正式论文表格：

- `case_code` 的占位提示；
- `total_source_count` 的“未公开”提示；
- `local_wall_duration_s`，它是控制程序墙钟时间，不等于官方 GUI 的程序运行时间。

摘要中的 `strategy` 应与本次命令一致。`evidence` 只是控制程序记录的来源提示，不能单凭该字符串认证官方结果；还须与GUI案例编码和模拟器导出文件关联。`accepted=false`、退出超时或异常时，先查看GUI与过程日志，不要重启脚本尝试续接已经进入的会话。未生成摘要并不表示本局从未执行动作。

真实案例编码必须从模拟器 GUI 或日志列表抄录。

## 10. 正式测试操作清单

### 每局开始前

- [ ] 已在 GUI 登录正确参赛队号；
- [ ] 已选择正确题号和演练/正式模块；
- [ ] 已确认测试开始；
- [ ] 已等待倒计时结束并看到接口就绪；
- [ ] 输出目录尚不存在；
- [ ] 只启动一个控制程序；
- [ ] 已冻结算法和参数。

### 每局结束后

- [ ] 已从 GUI 记录案例编码；
- [ ] 已记录结束原因；
- [ ] 已核对成功清除频道数；
- [ ] 已计算或核对 \(T/K\)；
- [ ] 已记录官方程序运行时间；
- [ ] 已导出官方加密日志；
- [ ] 已检查日志上传状态；
- [ ] 未将真实队号明文日志放入公开材料。

### 三次正式测试完成后

分别对问题3和问题4填写三行结果，共六行。不要使用占位案例编码，不要把本地合成实验结果写成正式测试结果。

## 11. 常见错误

| 错误 | 后果 | 正确处理 |
| --- | --- | --- |
| 把论文坐标轨迹上传到模拟器 | 官方接口不支持，无法执行 | 只将算法接入 `/measure` 和 `/clear` |
| 把 `no_signal` 当作无源 | 定向源和超接收半径源会漏检 | 完整覆盖后才建立 `absent-certified` |
| 把 `svd_deg` 当作精确方位 | 定位区域过度收缩，可能清除失败 | 按±1.005°覆盖题面误差及量化误差 |
| 用 `/clear` 频道判断测向机频道 | 频道状态错误、计时错误 | 只有 `/measure` 改变测向机频道 |
| 重试时换新 `request_id` | 同一动作可能重复执行 | 原请求内容和原ID一起重试 |
| 只检查 HTTP 200 | `accepted=false`动作并未执行 | 同时检查 HTTP状态和 `accepted` |
| 把拒绝响应的虚拟时间0当当前时间 | 时间状态被错误重置 | 只采用最近一次 `accepted=true` 的时间 |
| 正式测试填写 `K/N` | 正式接口不公开N | 正式表只填K和T/K |
| 用本地 JSONL 替代官方加密日志 | 不满足官方支撑材料要求 | 从 GUI 原名导出官方加密日志 |
| 同一输出目录重复运行 | 覆盖或混淆不同测试证据 | 每局使用全新的目录 |
| `unrecognized arguments: --strategy` | 启动的是旧版控制器或路径错误 | 在当前根仓库/完整支撑包运行 `official_run.py --help`，确认有field/refined选项 |
| `ModuleNotFoundError`（如field_policy） | 只复制了入口脚本或缺少新模块 | 完整更新仓库或重新完整解压支撑包，不从已清理的handoff代码目录启动 |
| `EnvironmentNameNotFound: py314` | 本机没有对应Conda环境 | 按快速开始创建环境，先执行本地检查，再开官方测试 |

## 12. 研究证据与官方证据的区分

当前仓库已有的本地合成运行可以用于：

- 检查算法能否从公开反馈完成动作闭环；
- 检查问题1、问题2的几何计算和定位逻辑；
- 比较策略的完整虚拟耗时；
- 检查失败清除恢复、边界搜索和通信重试；
- 生成论文中的非官方实验图表。

历史4430/3080/2200、refined开发2550及冻结研究10700次保持原文件与分母。新field开发2250次、冻结确认9400次（主组6000、压力1800、额外误差1600），主组每题1000世界。七组核心共34610次执行，不是独立世界数；fast辅助2000次另计。本地主组Q3均值258.9860→250.1703秒/源，Q4 542.4134→508.6006，两题1000/1000全清。Q4聚集smooth节省区间跨0，不称所有组显著或逐实例占优；所有这些改善都不是官方成绩。

20轮演练使用原refined/.22，Q3/Q4每轮等权均值237.9983/531.1776秒/源，pooled均值235.8227/519.3757，分别139/138源全清。全部成本由原始request→response独立复算；StrictReplay仅8/20完整匹配，其余遇到坐标/排序分岔即停止，不造反馈。脱敏原始文件支持 `official_analysis.py --handoff practice_data` 完整离线重建，不意味着20/20算法回放或新策略官方提升。源数、类型、case标签仅供退出后评价。

新策略的本地确认与合成实验不能证明：

- 官方案例中一定达到相同耗时；
- 官方案例分布与本地合成分布相同；
- 已经完成问题3、问题4的三次正式测试；
- 已经获得正式测试案例编码或原名加密日志；现有20个演练案例编码与正式材料应明确区分。

只有模拟器 GUI 的实际测试记录、官方导出的加密日志和按正式表格口径整理的六次正式结果，才能作为正式测试证据。

## 13. 参考文件

- `附件1.docx`：模拟器使用说明、GUI流程、测试次数、日志和截止规则；
- `附件2.docx`：HTTP接口、反馈字段、计时、重试和错误状态；
- `b_solution/official_run.py`：官方会话连接入口；
- `b_solution/client.py`：串行 HTTP 客户端和日志；
- `b_solution/planner.py`：论文搜索与清除策略；
- `b_solution/run.py`：本地合成环境入口；
- `b_solution/reproduce.txt`：本地研究复现命令和官方测试限制。
