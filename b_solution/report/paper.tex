\documentclass[UTF8,fontset=fandol,a4paper,zihao=-4]{ctexart}
\usepackage[a4paper,margin=2.5cm]{geometry}
\usepackage{amsmath,amssymb,amsthm,bm,booktabs,array,longtable,graphicx,float}
\usepackage{enumitem,xcolor,listings,hyperref,fancyhdr}
\usepackage{needspace}
\hypersetup{hidelinks,pdftitle={有界示向误差下无线电干扰源的覆盖认证搜索与快速清除},pdfauthor={}}
\setlength{\parindent}{2em}
\setlength{\parskip}{0.15em}
\linespread{1.15}
\setlist{itemsep=0.2em,topsep=0.3em,parsep=0pt}
\pagestyle{fancy}\fancyhf{}\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0pt}
\newtheorem{theorem}{结论}
\newtheorem{proposition}{命题}
\DeclareMathOperator*{\argmin}{arg\,min}
\DeclareMathOperator{\conv}{conv}
\newcommand{\dd}{\mathrm{d}}
\newcommand{\norm}[1]{\left\lVert#1\right\rVert}
\newcommand{\cross}[2]{\operatorname{cross}(#1,#2)}
\newcommand{\disk}{\mathcal D}
\newcommand{\poly}{\mathcal P}
\lstset{basicstyle=\ttfamily\fontsize{7.5}{9}\selectfont,breaklines=true,breakatwhitespace=false,
  columns=fullflexible,keepspaces=true,showstringspaces=false,frame=single,
  numbers=left,numberstyle=\tiny,numbersep=5pt,tabsize=4,
  keywordstyle=\color{blue!55!black},commentstyle=\color{black!65},
  stringstyle=\color{green!35!black},captionpos=t,xleftmargin=1.5em}
\input{generated/numbers.tex}
\setlength{\emergencystretch}{3em}
\input{generated/enhancement_numbers.tex}
\input{generated/ring_numbers.tex}
\input{generated/research_numbers.tex}
\input{generated/official_practice_numbers.tex}
\input{generated/field_numbers.tex}
\begin{document}
\begin{center}
{\LARGE\bfseries 有界示向误差下无线电干扰源的\\覆盖认证搜索与快速清除\par}
\vspace{0.6em}
\end{center}
\begin{center}\bfseries 摘要\end{center}
无线电干扰源的快速搜索、定位与清除面向危险或复杂环境下的现场排查任务。机器狗需要在半径1800米的圆形区域内自主行动，但干扰源的数量、频道、接收半径以及定向源的发射方向均未知；同时，测向误差不超过$1^\circ$，只有进入源20米范围内才能完成光学确认与清除。因此，本文将“全域不漏检、误差下可定位、反馈驱动调度和证书化停止”作为统一建模目标，构造“确定性覆盖保证+有界误差可行域+观测驱动路径规划”的求解框架。

\par\noindent\textbf{针对问题一，}将每次示向度表示为带正向约束的角度楔形，并将多次观测转化为半平面交，给出可行域为空、无界和有界三种情形的判别方法；通过枚举边界交点计算区域直径。进一步构造由合法测向观测产生的边长36米等边三角形反例，说明直径为$D$的区域不能简单用半径$D/2$的圆覆盖，最终以最小包围圆半径不超过19.9米作为保守的一次清除判据。

\par\noindent\textbf{针对问题二，}分析第二检测点对测向交会角、目标距离和移动代价的共同影响。利用首次收到信号所隐含的接收半径下界，推导由三个圆盘交集构成的安全候选区域，使候选点对允许的源位置具有接收保证；在此基础上，采用有限候选点和后验可行域包围圆半径进行评分，区分可证明的接收安全性与依赖采样的启发式定位效果。

\par\noindent\textbf{针对问题三，}针对全向干扰源建立原点加六点环的七点发现骨架，环半径取1200米，解析得到全域最坏覆盖距离约为968.901572米，较1000米接收下界保留约31.10米余量。对正观测采用有界误差半平面交逐步收缩位置域，并以局部测向和矩形光学覆盖完成清除；对未发现频道，只有在七个骨架位置全部获得有效无信号后才形成无源证书。

\par\noindent\textbf{针对问题四，}针对定向源的背面无信号问题，采用原点、半径940米内环和半径1870米错相外环组成的25点发现骨架，并将外十二边形剖分为36个闭三角形。引入辅助点$h=g+5n$，利用重心组合证明任意源位置和任意发射方向至少对应一个前向且距离小于999米的检测点，即使存在1米总位置误差仍保留4米前向余量。对定向源不把无信号误判为位置排除，而是结合正观测可行域、失信号恢复和光学后备完成定位清除。

\par\noindent\textbf{结果方面，}在用户提供的Q3、Q4各10轮官方模拟器演练中，原\texttt{refined}策略分别清除139/139和138/138个源，移动时间约占虚拟总时间的74.38\%和73.20\%。据此保留完整覆盖骨架和逐频道停止证书，并设计包含多初解、2-opt及单点搬移的\texttt{field}效率增强。冻结后的每问1000个新合成世界中，\texttt{field}相对原\texttt{refined}的平均单源耗时分别由\FieldQThreeControl{}、\FieldQFourControl{}秒降至\FieldQThreeTime{}、\FieldQFourTime{}秒，且两题均1000/1000全清。由于两组官方演练案例并非配对，新增策略的收益仅作为本地配对证据，不作官方因果结论；综合证书安全性和现有诊断，程序默认Q3使用\texttt{refined}、Q4使用\texttt{field}。

\noindent\textbf{关键词：}有界误差；交会定位；最小包围圆；覆盖证书；主动测向；定向源搜索
\newpage

\section{问题重述}
\subsection{问题背景}
无线电干扰源排查需要在有限时间内完成搜索、定位和清除。人工携带测向设备作业时，通常先根据示向度判断目标的大致方向，再通过多点交会缩小位置范围，最后近距离确认并清除。机器狗能够代替人员进入危险区域，但它只能根据当前位置的检测结果行动，无法预先获知全部目标的位置和数量。

本题的目标区域为半径1800米的圆域，机器狗从圆心出发，移动速度为5米/秒，测向机初始频道为1。每个频道至多有一个干扰源，有效接收半径在1000至1500米之间，示向误差不超过$1^\circ$。全向源在接收范围内均可检测，定向源还要求检测点位于其发射前方。进入目标20米范围后，机器狗可完成光学定位与清除。搜索过程因此同时受到几何位置、发射方向和操作耗时的影响\cite{problem}。

\subsection{问题要求}
根据题目条件，四个问题可整理为：
\begin{enumerate}
\item 已知检测点和示向度，建立交会定位区域，计算其直径，并判断直径圆能否覆盖该区域。
\item 已知一个全向源的首次示向度，给出第二检测点的选择方法和候选区域，使两次观测获得较好的定位效果。
\item 对数量未知的10至16个全向源，设计一只机器狗的搜索、定位与清除策略，在全清条件下缩短任务时间。
\item 将策略扩展到含定向源的情形，处理发射背面无信号、接收中断和区域边界等问题，并通过模拟器演练评价结果。
\end{enumerate}

\section{问题分析}
\subsection{问题一与问题二的分析}
示向度存在误差，单次检测给出的不是一条准确射线，而是一段角度范围。多次检测形成若干扇形楔区，它们的交集表示可能的源位置。问题一的重点是把角度条件转为可计算的平面约束；计算区域直径后，还需检查整个区域能否被一个清除圆盘覆盖。

第二检测点既影响两次测向的交会角，也影响移动距离和接收条件。沿首次示向方向前进虽然省路，但两条示向线接近平行时，距离方向的误差难以压缩。因此问题二先确定能够继续接收到信号的区域，再比较该区域内不同横向测点的定位效果。

\subsection{问题三与问题四的分析}
问题三中，机器狗不能以“暂时没有发现目标”结束任务。需要先构造覆盖整个目标区域的检测点集，再把未知频道的扫描任务与已知源的清除任务合并安排。局部定位使用问题一的可行区域，测向收缩不足时改用光学覆盖，完成从全域搜索到近距清除的衔接。

问题四增加了发射方向限制。即使距离很近，机器狗也可能位于发射背面；尤其是圆周上朝外发射的源，需要在目标区域外安排检测点。为此，本文用双环点集覆盖各个方向，并在信号中断后向最近一次有信号的位置回退，继续定位。

\begin{figure}[htbp]\centering
\includegraphics[width=0.94\textwidth]{figures/competition_taskmap.pdf}
\caption{四个问题的建模关系：交会区域支撑局部定位，覆盖点集支撑全域搜索。}
\label{fig:taskmap}
\end{figure}

\subsection{评价指标}
记总行程为$L$，检测次数为$M$，换频次数为$S$，失败清除次数为$F$，成功清除源数为$K$。按照题目计时规则，任务的虚拟总时间为
\begin{equation}\label{eq:time}
T=\frac{L}{5}+5M+S+3F+5K.
\end{equation}
成功清除的5秒已经包含光学定位与激光清除；清除指令不改变测向机频道。设实际源数为$N$，两项评价指标分别为清除率$K/N$和平均清除时间$T/K$。模型首先满足全清要求，再降低总时间。实验同时报告逐局等权平均$\frac1n\sum_jT_j/K_j$与汇总指标$\sum_jT_j/\sum_jK_j$，主策略比较采用前者。程序现实运行时间另行记录。

\section{模型假设}
在题目给出的源静止、频道唯一、发射方向固定等条件下，采用以下计算约定：
\begin{enumerate}
\item 同一坐标、同一频道的测向误差保持固定。不同地点的误差均在题目规定的范围内，不通过原地重复测量平均降噪。
\item 示向度保留两位小数，计算容差取$1.005^\circ$，包含$1^\circ$测向误差与舍入影响。
\item 用外切正180边形表示目标圆域，边界向外最大偏差小于0.275米；光学规划半径取19.9米，预留0.1米计算余量。
\item 在线策略只使用已返回的检测和清除结果。源总数与类型只在测试结束后用于计算评价指标。
\end{enumerate}
定向源的接收条件写为
\begin{equation}\label{eq:visible}
\norm{s-g_c}\le R_c,\qquad n_c^{\mathsf T}(s-g_c)\ge0.
\end{equation}
全向源只需满足距离条件。可检测且距离不超过5米时返回近源信息，可在原地清除；光学清除不受发射方向限制。

\section{符号说明}
\begin{table}[H]\centering\small
\caption{主要符号及含义}
\begin{tabular}{lll}\toprule
符号 & 含义 & 单位或范围\\\midrule
$\disk$ & 干扰源所在圆域 & 半径1800米\\
$c$、$g_c$ & 频道编号、该频道源坐标 & $c=1,\ldots,20$\\
$R_c$、$n_c$ & 接收半径、发射前向单位向量 & $R_c\in[1000,1500]$米\\
$s_i,\theta_i$ & 检测点及示向度 & 米、度\\
$\alpha$ & 示向度容差 & 计算取$1.005^\circ$\\
$\poly_c$ & 频道$c$的可能位置多边形 & 在线更新\\
$D(P),r_*(P),z_*(P)$ & 区域直径、最小包围圆半径及圆心 & 米\\
$V,V_c$ & 全部骨架索引、频道$c$已检测索引 & 有限集合\\
$L,M,S,F,K$ & 行程、检测、换频、失败清除及成功清除数 & 米、次\\
$T,N$ & 虚拟总时间、实际源数 & 秒、个\\\bottomrule
\end{tabular}
\end{table}


\section{问题一：交会区域直径与清除判据}
\subsection{从示向度到正向半平面交}
定义$u(\theta)=(\cos\theta,\sin\theta)$，$u_i^\pm=u(\theta_i\pm\alpha)$，二维叉积为$\cross ab=a_xb_y-a_yb_x$。一次观测对应
\begin{equation}\label{eq:wedge}
W_i=\left\{g:
\begin{aligned}
\cross{u_i^-}{g-s_i}&\ge0,\\
\cross{u_i^+}{g-s_i}&\le0,\\
u(\theta_i)^\mathsf T(g-s_i)&\ge0
\end{aligned}\right\}.
\end{equation}
前两个半平面在$\alpha>0$时已经给出正向楔形，第三个条件显式排除精确零误差情形下的背向射线。使用正弦、余弦表达使$359.9^\circ$和$0.1^\circ$之间的跨零问题无需分支处理。

原始交会区域为$P=\bigcap_i W_i$。若题意需要同时利用已知目标区域或距离上限，可再取交集；但添加先验后的区域不是原始无限楔形交，不能混淆二者的直径。

\subsection{空集、无界集和有界多边形的算法}
将全部约束写成$Ag\le b$。一个透明、易核查的实现如下。
\begin{enumerate}
\item 解二维可行性线性规划。无可行解时报告观测或条件矛盾，不返回虚假的零直径。
\item 分别最大化及最小化$x,y$。任一坐标目标无界，则非空区域无界，直径为$+\infty$；四个坐标上下界都有限则区域有界。
\item 对每对非平行边界直线求交，保留满足全部半平面约束的交点；去除数值重复点，按凸边界排序。
\item 求所有顶点对距离的最大值。单点直径为0，线段直径为两端点距离。
\end{enumerate}

配套\texttt{intersection.py}直接完成上述分类，不用任意大包围盒冒充原区域的有界性。若约束数为$m$，直线交点枚举并逐个检验的朴素复杂度为$O(m^3)$；通常只需少数测向，足够迅速。大规模情形可改为按极角排序的半平面交和旋转卡壳，几何阶段降至$O(m\log m)$，但这不是本题在线计算的瓶颈。

\begin{theorem}[直径的顶点刻画]\label{thm:diameter}
若$P=\conv\{v_1,\ldots,v_n\}$非空、有界，则
\begin{equation}D(P)=\max_{i,j}\norm{v_i-v_j}.\end{equation}
\end{theorem}
\begin{proof}
任取$p=\sum_i a_iv_i$与$q=\sum_j b_jv_j$，其中权重非负且各自和为1，有
\[
\norm{p-q}=\norm{\sum_{i,j}a_ib_j(v_i-v_j)}
\le\sum_{i,j}a_ib_j\norm{v_i-v_j}\le\max_{i,j}\norm{v_i-v_j}.
\]
反向不等式由各顶点本身属于$P$得到。
\end{proof}

\subsection{直径圆不能总是覆盖：一个真实可实现的反例}
边长为$D$的等边三角形直径就是$D$，最小包围圆半径却是$D/\sqrt3>D/2$。这不仅是任意多边形反例，还能由本题$\pm1^\circ$测向直接生成。

取$D=36$，$A=(0,0)$、$B=(36,0)$、$C=(18,18\sqrt3)$。依次对三条逆时针有向边取单位方向$e_j$，令
\begin{equation}
S_j=V_j-Le_j,\qquad L=18\sqrt3\cot2^\circ-18.
\end{equation}
测向读数取边方向加$1^\circ$，即$1^\circ,121^\circ,241^\circ$。每个楔形下边界沿对应边、上边界经过对角顶点；三个楔形都包含该三角形，而三条下边界的内半平面交恰为该三角形。所有三角形点距对应检测站小于1000米，故可取合法接收半径1000米。

实际计算得到直径约36.000000米，最小包围圆半径约20.784610米。于是即使$D<40$，仍不一定能在某一点以20米光学半径保证一次清除。
\begin{figure}[H]\centering
\includegraphics[width=\textwidth]{figures/geometry.pdf}
\caption{直径为36米的可实现三角形反例。任何半径18米的圆都无法覆盖三顶点；正确包围半径约20.785米。}
\end{figure}

\subsection{用最小包围圆直接连接定位与清除}
定义
\begin{equation}\label{eq:mec}
r_*(P)=\min_z\max_{p\in P}\norm{p-z},\qquad z_*(P)=\argmin_z\max_{p\in P}\norm{p-z}.
\end{equation}
圆盘是凸集，包含多边形所有顶点即包含整个多边形。最小包围圆由一个、两个或三个边界支持点确定，可用随机增量支持点算法求解\cite{welzl,zhang}。本文实现对近共线三点重算高精度行列式，并在最终半径上保留向外余量。

平面Jung界为
\begin{equation}\label{eq:jung}
\frac{D(P)}2\le r_*(P)\le\frac{D(P)}{\sqrt3}.
\end{equation}
两点支持时左端取等；三点支持时圆心在支持三角形内，三段圆心角中最大的一段至少$120^\circ$，对应弦长至少$\sqrt3r_*$，得到右端。等边三角形取右端等号。因此：
\begin{itemize}
\item $r_*(P)\le20$是存在一次保证清除点的充要几何条件；执行点取$z_*(P)$。
\item 仅使用直径时，$D(P)\le20\sqrt3$是充分条件，$D(P)>40$则必不可能一次保证覆盖。
\item $20\sqrt3<D(P)\le40$时必须进一步算包围圆，不能凭直径直接清除。
\end{itemize}

\subsection{在线使用的保守多边形}
在问题三、四中，每次有效示向度还说明$\norm{g-s_i}\le1500$。为保持纯线性裁剪，可使用安全松弛
\begin{equation}
u(\theta_i)^\mathsf T(g-s_i)\le1500.
\end{equation}
这不是把接收半径错误设为1500，而是利用其已知上界。若初始外切多边形为$A_{180}$，则维护
\begin{equation}\label{eq:poly}
\poly_c=A_{180}\cap\bigcap_{i\in I_c}\left(W_i\cap\{g:u(\theta_i)^\mathsf T(g-s_i)\le1500\}\right).
\end{equation}
真实源初始在$A_{180}$内，每条新增约束也包含它，故归纳得到$g_c\in\poly_c$。一次观测裁剪四个半平面，$m$次观测最多形成$180+4m$条边，复杂度$O(180m+m^2)$。空交集是需要定位原因的异常，而不是允许重置状态的信号。

\section{问题二：第二检测点的安全候选区域与选点策略}
\subsection{单次测向为什么没有唯一最优第二点}
只有角度而没有距离时，目标位于长而窄的扇形中。若第二点沿首次示向方向移动，两条示向线可能几乎平行或反平行，径向误差难以压缩。若仅为了接近$90^\circ$交会而绕行很远，则会增加移动时间，未必有利于题目总目标。

令$r_i=\norm{g-s_i}$、$u_i=(g-s_i)/r_i$、$n_i=(-u_{iy},u_{ix})$。对小误差线性化，有
\begin{equation}\label{eq:strip}
|n_i^\mathsf T\delta g|\le\alpha r_i.
\end{equation}
两条误差带交形成的平行四边形面积近似为
\begin{equation}\label{eq:area}
A\approx\frac{4\alpha^2r_1r_2}{|\sin\beta|},
\end{equation}
其中$\beta$为两条真实视线夹角，$\alpha$以弧度计。固定距离时，$90^\circ$交会最有利；距离也变化时，应同时考虑缩短$r_2$、避免共线及移动代价。该式是局部解释，不能代替精确楔形裁剪。

若额外采用独立高斯角误差的辅助似然，才可写出Fisher信息$J=\sigma^{-2}\sum_i n_in_i^\mathsf T/r_i^2$及$\det J=\sin^2\beta/(\sigma^4r_1^2r_2^2)$。没有这种似然假设时，这至多是加权最小二乘的局部信息代理，不能当成题目提供的真实误差统计模型；移动单站和交叉测向研究也表明，观测几何与测向误差会直接影响无源定位精度\cite{qin,li}。最终安全性仍由式\eqref{eq:poly}保证。

\subsection{利用首次已接收事实推导三圆交}
取第一次检测点$S_1$为局部原点，首次读数方向为$u$，法向为$n$，第二点写为
\begin{equation}S_2=S_1+tu+bn.\end{equation}
若可能真实距离为$r$，首次已经接收说明
\begin{equation}R\ge\max(1000,r),\end{equation}
而不仅仅是$R\ge1000$。设$F_1$为当前全部可能源位置，则保证全向源在第二点仍有信号或返回近源的精确鲁棒条件为
\begin{equation}\label{eq:receive}
S_2\in\bigcap_{g\in F_1} B\bigl(g,\max(1000,\norm{g-S_1})\bigr).
\end{equation}
用完整扇形$r\in[0,1500]$、角差$\varphi\in[-\alpha,\alpha]$覆盖真实$F_1$，可得闭式充分候选域。

\begin{theorem}[三圆交接收保证]\label{thm:lens}
令$u_\pm=u(\theta_1\pm\alpha)$。以下候选域内的任一点，对整个放宽扇形都能保证全向接收：
\begin{equation}\label{eq:lens}
\mathcal C=S_1+\bigl[B(0,1000)\cap B(1000u_-,1000)\cap B(1000u_+,1000)\bigr].
\end{equation}
对这一完整扇形，式\eqref{eq:lens}也恰好是式\eqref{eq:receive}的交集；若再利用圆域等更强先验，它可能不是最大的安全候选域。
\end{theorem}
\begin{proof}
写$q=S_2-S_1$。必要性由$r=0$和$r=1000,\varphi=\pm\alpha$给出。反之，两端圆约束意味着$q^\mathsf Tu_\pm\ge\norm q^2/2000\ge0$。小于$180^\circ$的中间短弧上，$q^\mathsf Tu_\varphi$的最小值在端点，因此对所有允许角度都满足同样下界。若$r\le1000$，距离$\norm{q-ru_\varphi}$关于$r$是凸函数，两端$r=0,1000$均不超过1000，内部也不超过1000；若$r\ge1000$，
\[
\norm{q-ru_\varphi}^2-r^2=\norm q^2-2r q^\mathsf Tu_\varphi\le0.
\]
故距离不超过$\max(1000,r)$，由首次接收所隐含的半径下界即可保证可检测。
\end{proof}

\subsection{显式候选区域与可执行搜索}
在$(t,b)$局部坐标中，三圆交等价于
\begin{equation}\label{eq:lenscoord}
\left\{\begin{aligned}
t^2+b^2&\le1000^2,\\
(t-1000\cos\alpha)^2+(b-1000\sin\alpha)^2&\le1000^2,\\
(t-1000\cos\alpha)^2+(b+1000\sin\alpha)^2&\le1000^2.
\end{aligned}\right.
\end{equation}
令
\begin{equation}
b_{\max}(t)=\min\left\{\sqrt{1000^2-t^2},\sqrt{1000^2-(t-1000\cos\alpha)^2}-1000\sin\alpha\right\}.
\end{equation}
根号有定义且$b_{\max}\ge0$时，$0<t<1000$、$0<|b|\le b_{\max}(t)$形成首次方位两侧的横向候选区。排除$b=0$用于避免把共线点作为唯一方案，而非禁止为了接近目标沿轴行进。

程序按下式构造42个候选：
\begin{equation}
\begin{aligned}
t&\in\{125,250,375,500,625,750,875\}\ \text{米},\\
b&=\pm\kappa b_{\max}(t),\qquad \kappa\in\{1/4,1/2,3/4\}.
\end{aligned}
\end{equation}
对每个候选模拟允许的第二读数，计算新多边形最小包围圆；优先选择采样最坏半径较小的点，可再以本步移动和检测代价打破并列。所有这些点的接收安全性来自定理\ref{thm:lens}，不是来自采样。

\begin{figure}[H]\centering
\includegraphics[width=\textwidth]{figures/q2_lens.pdf}
\caption{第二点的三圆交候选区域。横向基线改善交会条件，但候选排序仍需兼顾移动耗时和未知径向距离。}
\end{figure}

在$S_1=(0,0),\theta_1=0$的归一化示例中，取60个径向位置、3个首次真实角差和3个第二次测向误差进行比较。程序得到采样最佳几何候选约为$(750,-496.078)$米，采样最坏包围半径约65.426米；共线对照$(750,0)$的对应值约375.231米。两者本步耗时分别约184.844秒和155秒。这说明增加约29.844秒移动检测成本可显著改善此例的径向辨识，但第二次测向后仍未必能一次清除。

该示例是候选域与评分算法的演示，不是题目给定正式案例，也不是连续最坏情形或总时间的全局最优解。二维方位误差的连续极值、未知距离分布及后续代价若未严格处理，不能将网格最小值写成“最优定位策略”。

\input{competition_q3.tex}
\input{competition_q4.tex}

\begin{figure}[htbp]\centering
\includegraphics[width=0.88\textwidth]{figures/competition_dispatch.pdf}
\caption{机器狗根据公开反馈循环执行扫描、定位和停止判断。}
\label{fig:dispatch}
\end{figure}

\section{模型求解与结果分析}
\subsection{合成环境与实验设置}
本地模拟器按照题目公开的动作和反馈规则生成测试场景，实验中源数取10至16，位置位于半径1800米圆域内，接收半径取1000至1500米。问题三使用全向源，问题四包含定向源。策略只读取接口返回的信息，源坐标、类型和总数仅用于实验结束后的统计。

\begin{figure}[htbp]\centering
\includegraphics[width=0.86\textwidth]{figures/competition_costs.pdf}
\caption{主测试组的移动与操作成本分解。}
\label{fig:costs}
\end{figure}

\input{official_practice.tex}
\input{field_results.tex}
\input{bound_proof.tex}
\section{模型检验}
\subsection{几何与算法检验}
为检验模型的可行性，分别对交会区域、七点环和双环骨架进行数值核对。测试包含边界点、角度跨越$0^\circ$、重合检测点和近共线观测。程序重新计算每一步的移动、检测、换频和清除时间，并将结果与式\eqref{eq:time}核对。对于已经发现的频道，只有收到清除成功反馈才改变频道状态；对于尚未发现的频道，必须完成全部骨架位置检测后才可判断无源。
\begin{table}[H]\centering\small
\caption{模型与算法检验结果}
\begin{tabular}{p{4.2cm}p{3.0cm}p{6.6cm}}\toprule
检验项目 & 检验规模 & 结果\\\midrule
最小包围圆交叉计算 & 504组点集 & 与支持点枚举结果一致\\
七点环与真实源包含 & 2000组、每组4次测向 & 全部满足可行域包含条件\\
双环方向覆盖 & 36个闭三角形 & 边界、顶点和方向扰动均满足距离及投影条件\\
频道状态与终止 & 两种题型完整流程 & 无任务遗漏，终止条件一致\\
动作计时 & 主测试逐世界记录 & $T=L/5+5M+S+3F+5K$成立\\\bottomrule
\end{tabular}
\end{table}
\subsection{策略结果与成本分析}
图\ref{fig:costs}给出了主测试组的移动成本和操作成本。移动仍是总时间的主要组成部分，因此联合任务排序优先减少跨任务绕行。Q3使用refined作为默认策略；Q4使用field的路线组合。两题策略均保留完整覆盖骨架和逐频道终止条件，清除率不以牺牲全清为代价。
\begin{figure}[htbp]\centering
\includegraphics[width=0.88\textwidth]{figures/research_performance.pdf}
\caption{两类问题的逐世界单源耗时分布。}
\label{fig:performance}
\end{figure}
\subsection{结果讨论}
在1000个主测试世界中，field相对refined的Q3平均单源时间节省8.8158秒，Q4节省33.8128秒；两题均完成1000/1000个世界的全清。Q3的官方演练结果显示field在另一组案例中耗时较高，因此最终程序采用Q3 refined、Q4 field的分题配置。该选择同时考虑了实测时间、覆盖结构和算法复杂度。

\section{模型评价与结论}

本文完成了四个问题的建模与求解。问题一由正向半平面交求区域直径，最小包围圆而非直径圆是一次保证清除的判据；问题二给出有全向接收保证的三圆交候选域，并据交会条件选择第二检测点。问题三以七点收缩环完成全向源搜索，问题四以25点双环完成定向源搜索，逐频道证书负责判断清除和停止。
\subsection{模型优缺点与适用范围}
模型将覆盖保证与任务调度分开处理，既能证明搜索不漏检，又能用统一时间模型比较路线效率。Q3默认使用原\texttt{refined}策略，Q4默认使用\texttt{field}策略；field仍可通过命令行显式调用。局限在于任务路线没有全局最优证明，实验误差场属于合成设置，官方隐藏参数尚未公开。模型可推广到不同区域半径、接收下界和检测骨架，但每次推广都需重新验证覆盖条件与清除证书。
\subsection{最终策略与结果}
用户提供的官方模拟器演练中，原\texttt{refined}在Q3完成139/139个源、Q4完成138/138个源。独立本地配对试验中，field相对于refined的Q3平均单源时间由258.9861降至250.1703秒，Q4由542.4134降至508.6006秒；两题主组均1000/1000全清。结合Q3官方非配对演练中field的较慢表现，最终采用Q3\texttt{refined}、Q4\texttt{field}。

\section*{AI工具使用声明}
本研究过程中使用了AI工具，参与核心模型推导、代码实现、实验设计、结果整理、独立自动审查及论文写作，详细过程见支撑材料中的《AI工具使用详情》。本稿尚无真实参赛队完成逐项人工核验的记录，不将自动审查写作人工审查；该声明用于如实披露研究过程，不能替代正式提交前应完成的参赛队责任。相关披露和论文编排遵循竞赛组委会的论文规范及人工智能工具使用规定\cite{format,airule}。

\begin{thebibliography}{99}
\raggedright
\bibitem{problem} 全国大学生数学建模竞赛组委会. 2026年高教社杯全国大学生数学建模竞赛B题：无线电干扰源的快速自动定位与清除，以及附件1、附件2[Z]. 2026. \url{https://www.mcm.edu.cn/html_cn/node/27b6e148f8113f09b0269f64a02629fb.html}.
\bibitem{welzl} Welzl E. Smallest enclosing disks (balls and ellipsoids)[C]//New Results and New Trends in Computer Science. Lecture Notes in Computer Science, 555. Springer, 1991: 359--370. DOI: 10.1007/BFb0038202.
\bibitem{zhang} 张媛, 缪相林. 基于Welzl算法的无人机空中能量补给算法[J]. 微型电脑应用, 2025, 41(06): 288--291.
\bibitem{qin} 秦明峰, 郝青儒, 范广伟. 运动单站无源定位性能分析[J]. 无线电工程, 2014, 44(04): 50--53.
\bibitem{li} 李亚军. 机载塔康无源定位方法研究[J]. 无线电工程, 2019, 49(03): 229--233.
\bibitem{wang} 王沛栋, 唐功友, 杨熙鑫, 等. 一种求解旅行商问题的改进蚁群算法[J]. 中国海洋大学学报（自然科学版）, 2013, 43(01): 93--97. DOI: 10.16441/j.cnki.hdxb.2013.01.014.
\bibitem{ren} 任进, 李一博, 闵畅. 基于改进粒子群算法的无线传感器网络覆盖优化[J]. 无线电工程, 2024, 54(12): 2841--2849.
\bibitem{jia} 贾政敏, 陈清阳, 鲁亚飞, 等. 多无人机协同不规则区域覆盖与路径规划方法[J]. 无人系统技术, 2026, 9(02): 14--25. DOI: 10.19942/j.issn.2096-5915.2026.02.14.
\bibitem{format} 全国大学生数学建模竞赛组委会. 全国大学生数学建模竞赛论文格式规范（2026年修订稿）[EB/OL]. \url{https://www.mcm.edu.cn/html_cn/node/4cd596519c9eb9fbd866398f6df0caa3.html}.
\bibitem{airule} 全国大学生数学建模竞赛组委会. 全国大学生数学建模竞赛人工智能工具使用规定（2026年试行）[EB/OL]. \url{https://www.mcm.edu.cn/html_cn/node/fef94648f2836ab6cc81586f4c38512b.html}.
\end{thebibliography}
\label{endbody}
\clearpage
\appendix
\input{appendix.tex}
\end{document}
